diff --git a/okl/TensorProductAll.okl b/okl/TensorProductAll.okl
index 4795139..d4b3d0e 100644
--- a/okl/TensorProductAll.okl
+++ b/okl/TensorProductAll.okl
@@ -31,11 +31,19 @@
 	      double *c = C + l*LDC;
 	      double *tK = tmpK;
 	      tK[off + k] = 0.0;
+              double sum=0;
+              double AA[4],bb[4];
+#pragma unroll(4)
+              for(int kk=0; kk < K; ++kk) {
+                const int offA = kk * MPad + offsetA;
+                const int indB = K * (j + kk * K) + i;
+                AA[kk] = At[offA + k];
+                bb[kk] = b[indB];
+              }
 	      for (int kk = 0; kk < K; ++kk) {
-		const int offA = kk * MPad + offsetA;
-		const int indB = K * (j + kk * K) + i;
-		tK[off + k] += At[offA + k] * b[indB];
+		sum += AA[kk]*bb[kk];
 	      }
+              tK[off + k] = sum;
 	    }
 	  }
 	}
@@ -49,12 +57,20 @@
 	    double *tJ = tmpJ; 
 	    const int off = MPad * (i + k * K);	    
 	    // Initialize to zero
-	    tJ[off + j] = 0.0;
+	    //tJ[off + j] = 0.0;
+            double sum=0;
+            double AA[4],bb[4];
+#pragma unroll(4)
+            for(int jj=0; jj < K  ; ++jj) {
+              const int offA = jj * MPad + offsetA;
+              const int indK = MPad * (jj + i * K) + k;
+              AA[jj] = As[offA+j];
+              bb[jj] = tK[indK];
+            }
 	    for (int jj = 0; jj < K; ++jj) {
-	      const int offA = jj * MPad + offsetA;
-	      const int indK = MPad * (jj + i * K) + k;
-	      tJ[off + j] += As[offA + j] * tK[indK];
-	    }	    
+	      sum += AA[jj] * bb[jj];
+	    }
+            tJ[off+j]=sum;	    
 	  }
 	}
       }
@@ -71,17 +87,22 @@
 	    double accum;
 	    accum=0;
 	    // Multiply-accumulate in r-direction
-//#pragma omp simd
+            double AA[4],bb[4];
+#pragma unroll(4)
+            for(int ii=0; ii < K; ++ii) {
+              const int offA = ii * MPad + offsetA;
+              const int indJ = MPad * (ii + k * K) + j;
+              AA[ii] = Ar[offA + i];
+              bb[ii] = tJ[indJ];
+            }
 	    for (int ii = 0; ii < K; ++ii) {
-	      const int offA = ii * MPad + offsetA;
-	      const int indJ = MPad * (ii + k * K) + j;
-	      accum += Ar[offA + i] * tJ[indJ];
+	      accum += AA[ii]*bb[ii];
 	    }
 	    // Write or add to C
 	    //if (addToC) {
 	    //  c[offC + i] += accum;
 	    //} else {
-	      c[offC + i] = accum;
+	    c[offC + i] = accum;
 	    //}
 	  }
 	}
